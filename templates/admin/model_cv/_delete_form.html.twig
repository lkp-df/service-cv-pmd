<form method="post" action="{{ path('admin_model_cv_delete', {'id': model_cv.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ model_cv.id) }}">
    <button class="btn">Delete</button>
</form>
