{% extends 'base.html.twig' %}

{% block title %}ModelCv{% endblock %}

{% block body %}
    <h1>ModelCv</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ model_cv.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ model_cv.designation }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ model_cv.prix }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ model_cv.image }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ model_cv.createdAt ? model_cv.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_model_cv_index') }}">back to list</a>

    <a href="{{ path('admin_model_cv_edit', {'id': model_cv.id}) }}">edit</a>

    {{ include('admin_model_cv/_delete_form.html.twig') }}
{% endblock %}
