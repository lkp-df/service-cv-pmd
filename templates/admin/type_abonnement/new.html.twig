{% extends 'admin/base.html.twig' %} {% import "macros/link.html.twig" as link %} {% import "macros/button.html.twig" as btn %} {% block title %}New Type Abonnement{% endblock %} {% block body %}
<div class="card">
    <div class="card-header card-outline card-primary">
        <h3>
            Création d'un nuveau Type Abonnement
        </h3>
    </div>
    {{form_start(form)}}
    <div class="card-body">
        {{form_row(form.designation)}} {{form_row(form.montant)}} {{form_row(form.nombreCv)}}
        <div class="btn-group">
            {{link.back(path('admin_type_abonnement_index'))}} {{btn.add()}}
        </div>
    </div>
    {{form_end(form)}}
</div>{% endblock %}