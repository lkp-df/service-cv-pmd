<form method="post" action="{{ path('admin_user_for_cv_delete', {'id': user_for_cv.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_for_cv.id) }}">
    <button class="btn">Delete</button>
</form>
