{% extends "admin/base.html.twig" %} {% import "macros/button.html.twig" as btn %} {% import "macros/link.html.twig" as link %} {% block title %} Edit User Langue {% endblock %} {% block body %}
<div class="card card-outline card-success">
    <div class="card-header">
        Logiciel Actuel: {{ logiciel.designation }} &nbsp;Pourcentage: &nbsp; {{ logiciel.getNiveauPourcent }} %


    </div>
    {{ form_start(form) }}
    <div class="card-body">
        <div class="form-group">
            {{ form_row(form.designation) }}
        </div>
        <div class="form-group">
            {{ form_row(form.niveau_pourcent) }}
        </div>
        <div class="btn-group" role="group">
            {{ link.back(path('cv_edit',{'id':userForcv.id})) }} {{ btn.update() }}
        </div>
    </div>
    {{ form_end(form) }}
    <div class="card-footer"></div>
</div>
{% endblock %}