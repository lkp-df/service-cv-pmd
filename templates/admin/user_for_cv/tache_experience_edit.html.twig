{% extends "admin/base.html.twig" %}
{% import "macros/button.html.twig" as btn %}
{% import "macros/link.html.twig" as link %}
{% block title %}
	Edit Tache Professionnelle
{% endblock %}
{% block body %}
	<div class="card card-outline card-success">
		<div class="card-header">
			Tache Selectionnée :
			{{ tache.description }}
		</div>
		{{ form_start(form) }}
		<div class="card-body">
			<div class="form-group">
				{{ form_row(form.description) }}
			</div>
			<div class="btn-group" role="group">
				{% if is_granted('ROLE_ADMIN') %}
					{{ link.back(path('cv_edit_exp',{'id':userForcv.id,'id_exp':experience.id})) }}
					{{ btn.update() }}
				{% elseif is_granted('ROLE_USER') %}
					{{ link.back(path('cv_edit_exp_client',{'id':userForcv.id,'id_exp':experience.id})) }}
					{{ btn.update() }}
				{% endif %}
			</div>
		</div>
		{{ form_end(form) }}
		<div class="card-footer"></div>
	</div>
{% endblock %}
